import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Settings | Dashboard",
  description: "This is settings page for dashboard",
};

export default function Settings() {
  return (
    <>
      <h1>Hello, Settings!</h1>
    </>
  );
}
